// Author: Nikhil Arora
import java.util.*;

// calculates minimum twists required 
// to position and orient a cubie
class Manhattan{
	public int compute(Cube c, Point p){
		// TO DO"
		// loop through all possible 26 points (in h)
		// given a cubie:
		// do an A star or some least cost path analysis on each 
		// point with the cubie at that point
		// use getCubieValue to fetch cubie info at a given point
		// use cubieSolved as goal test
		// potentially change some cubie functions in Cube to private
		// potentially make cubieSolved take a Point instead of a Cubie
		// potentially remove location variable from Cubie class
		// develop an admissible heuristic for this search if A star is used
		return 0;
	}
}